# TODO — Issue #55 标准模式多选邮箱批量拉取（latest-only）

> 关联 Issue：GitHub Issue #55
> 关联本地方案：`session/files/issue55-standard-batch-fetch-plan.md`
> 关联 PRD：`docs/PRD/2026-04-25-标准模式多选邮箱批量拉取PRD.md`
> 关联 FD：`docs/FD/2026-04-25-标准模式多选邮箱批量拉取FD.md`
> 关联 TD：`docs/TD/2026-04-25-标准模式多选邮箱批量拉取TD.md`
> 关联 TDD：`docs/TDD/2026-04-25-标准模式多选邮箱批量拉取TDD.md`
> 优先级：P1（高频效率增强）
> 创建时间：2026-04-25
> 当前阶段：核心实现、全量测试与最终语义收口均已完成

---

## 任务总览

| 序号 | 任务 | 优先级 | 文件 | 当前状态 |
|------|------|--------|------|----------|
| 0 | 测试基线与红灯确认 | P0 | `tests/test_batch_fetch_frontend_contract.py`、`tests/test_batch_fetch_email_api_contract.py`、`tests/batch-fetch/*` | 已完成 |
| 1 | 标准模式批量栏入口补齐 | P0 | `templates/index.html` | 已完成 |
| 2 | 批量拉取主入口与账号解析 | P0 | `static/js/main.js` | 已完成 |
| 3 | latest-only 批量执行链路 | P0 | `static/js/main.js` | 已完成 |
| 4 | 缓存回写与当前视图刷新 | P0 | `static/js/main.js` | 已完成 |
| 5 | 文案、确认与进度提示接入 | P1 | `static/js/i18n.js`、`static/js/main.js` | 已完成 |
| 6 | Python 契约测试打绿 | P0 | `tests/test_batch_fetch_frontend_contract.py`、`tests/test_batch_fetch_email_api_contract.py` | 已完成 |
| 7 | Jest 环境补齐并执行行为测试 | P1 | `package.json` / 现有前端测试依赖、`tests/batch-fetch/*` | 已完成 |
| 8 | 文档回写与 WORKSPACE 记录 | P1 | 本地方案、PRD/FD/TD/TDD、`WORKSPACE.md` | 已完成 |

---

## 设计边界（本轮固定）

1. 只覆盖**标准模式**，只改 `batchActionBar`，不改 `compactBatchActionBar`。
2. V1 只做 **latest-only** 轻量批量拉取，默认范围固定为 `inbox + junkemail`。
3. V1 不新增后端 selected batch fetch API，继续复用 `GET /api/emails/<email_addr>`。
4. 批量完成后**不自动切换当前账号**。
5. 当前先**不设单次选择上限**，但实现上仍应保持串行或极小并发思路。

---

## 任务详情

### Task 0：测试基线与红灯确认

**目标**：先让 RED 落在真实实现缺口上，避免后续开发偏航。

- [x] 创建 Python 前端契约测试
- [x] 创建 Python 邮件 API 契约测试
- [x] 创建独立 Jest 行为测试套件
- [x] 首次执行 Python 测试并确认 3 个红灯命中真实缺口
- [x] 首次执行 Jest 并确认当前阻塞点为 `jest-environment-jsdom`

**当前结论**：

1. RED 阶段的 3 个实现缺口都已经被消除
2. Python 契约测试与 Jest 行为测试现已全部转绿

---

### Task 1：标准模式批量栏入口补齐

**目标**：在标准模式批量操作栏提供明确入口，并保持现有视觉层级。

- [x] 在 `batchActionBar` 增加”批量拉取邮件”按钮
- [x] 按钮样式使用 `ghost`
- [x] 不改 `compactBatchActionBar`
- [x] 不影响现有”刷新 Token / 删除”入口

---

### Task 2：批量拉取主入口与账号解析

**目标**：补齐前端入口函数，沿用现有批量语义解析已选账号。

- [x] 新增 `showBatchFetchConfirm()`
- [x] 新增 `resolveSelectedAccountsForBatchFetch()`
- [x] 解析逻辑扫描 `Object.values(accountsCache)` 下的各分组数组
- [x] 仅提取本轮所需最小字段：`id / email / account_type / provider`

---

### Task 3：latest-only 批量执行链路

**目标**：在不新增后端批量接口的前提下，复用现有邮件接口完成批量预热。

- [x] 新增 `batchFetchSelectedEmails()`
- [x] 新增单账号 folder 拉取 helper
- [x] 对每个账号执行 `inbox + junkemail`
- [x] 请求参数保持 `skip=0&top=10`
- [x] 失败项聚合但不中断整批

---

### Task 4：缓存回写与当前视图刷新

**目标**：把拉取结果写回现有状态，而不是平行造一套新状态。

- [x] 将结果回写到 `emailListCache[${email}_${folder}]`
- [x] 保持 `has_more / skip / method / emails` 结构一致
- [x] 仅在 `currentAccount + currentFolder` 双命中时刷新右侧邮件列表
- [x] 批量完成后保持 `currentAccount` 不变

---

### Task 5：文案、确认与进度提示接入

**目标**：补齐入口文案与执行反馈，保证用户能看见过程与结果。

- [x] 在 `i18n.js` 中加入”批量拉取邮件”相关文案
- [x] 接入确认提示文案
- [x] 接入进行中进度提示（如 `0 / N`）
- [x] 接入完成汇总文案（成功数 / 失败数 / 失败账号）

---

### Task 6：Python 契约测试打绿

**目标**：先把已经落盘的 Python RED 用例推进为 GREEN。

- [x] `tests/test_batch_fetch_frontend_contract.py` 全部通过
- [x] `tests/test_batch_fetch_email_api_contract.py` 保持通过
- [x] 确认红灯已从”入口缺失”转为全部消除，而不是通过删断言规避

---

### Task 7：Jest 环境补齐并执行行为测试

**目标**：让 B 层前端行为测试真正跑起来，而不是卡在环境初始化阶段。

- [x] 补齐 `jest-environment-jsdom`
- [x] 重新执行 `tests/batch-fetch/` 套件
- [x] 根据实际失败结果继续修正 `main.js`
- [x] 保持该套件仍独立于现有 `tests/compact-poll/`

---

### Task 8：文档回写与 WORKSPACE 记录

**目标**：开发推进过程中保持文档链与实际状态同步。

- [x] 本地方案文件补齐 TODO 回链与实现进度
- [x] 必要时回写 PRD / FD / TD / TDD 的状态描述
- [x] 将关键动作和结果持续记录到 `WORKSPACE.md`

---

## 当前状态

| 任务 | 状态 |
|------|------|
| Task 0 测试基线与红灯确认 | ✅ 已完成 |
| Task 1 标准模式批量栏入口补齐 | ✅ 已完成 |
| Task 2 批量拉取主入口与账号解析 | ✅ 已完成 |
| Task 3 latest-only 批量执行链路 | ✅ 已完成 |
| Task 4 缓存回写与当前视图刷新 | ✅ 已完成 |
| Task 5 文案、确认与进度提示接入 | ✅ 已完成 |
| Task 6 Python 契约测试打绿 | ✅ 已完成 |
| Task 7 Jest 环境补齐并执行行为测试 | ✅ 已完成 |
| Task 8 文档回写与 WORKSPACE 记录 | ✅ 已完成 |

---

## 实现后审查收尾（已完成）

1. `static/js/main.js`
   - 批量拉取进度已改为按账号完成数更新
   - 成功/失败汇总已改为账号级统计
2. `static/js/main.js`
   - 未选中账号时的错误提示已改为批量拉取邮件专用文案
3. `static/js/main.js`
   - 批量拉取成功后已补上 `account_summary` 回写
   - 若存在 `account_summary`，现已调用 `syncAccountSummaryToAccountCache(...)` 回写账号缓存

---

## 最终 diff 审查收口（已定）

1. `static/js/main.js`
   - 当某个账号出现“部分成功”（如 `inbox` 成功、`junkemail` 失败）时
   - 当前已按用户选择收口为：
     - 只要任一 folder 成功，即按该账号成功处理
     - 保留已成功 folder 的缓存
     - 不再把该账号归入失败列表
2. 当前结果：
   - 该语义决策已落实到实现
   - Issue #55 定向回归已重新通过

---

## 风险与注意事项

1. 不能把本期需求误实现成“多账号混合邮件浏览”，否则会越过当前 TD/FD 已经收紧的边界。
2. `accountsCache` 不是按 ID 直索引，账号解析如果偷懒会直接导致跨分组选中失效。
3. 后续继续维护时仍应保留 `jest-environment-jsdom` 依赖，避免行为测试再次退回环境层假红。
