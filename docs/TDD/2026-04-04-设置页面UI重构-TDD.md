# TDD: 设置页面整体 UI 重构 — Tab 化 + 临时邮箱配置区分离

> 基于 FD v0.1 (`docs/FD/2026-04-04-设置页面UI重构-FD.md`) 和 TD v0.1 (`docs/TD/2026-04-04-设置页面整体UI重构-TD.md`)，本文档描述完整的测试用例设计和实现计划。

---

## 1. 测试分层策略

| 层级 | 文件 | 框架 | 测试目标 | 用例数 |
|------|------|------|---------|--------|
| A. 后端新 Key 契约 | `tests/test_settings_tab_refactor_backend.py` | Python unittest | 3 个新 settings key 的 GET/PUT/默认值/数据隔离/同步接口 | 14 |
| B. 前端契约 | `tests/test_settings_tab_refactor_frontend.py` | Python unittest | HTML 元素存在性（Tab、Provider radio、只读字段）、JS 函数存在性 | 12 |

**执行顺序**：先跑 A 类（后端），再跑 B 类（前端契约）。

**不在本 TDD 范围内**：Tab 切换动态交互（需浏览器环境，可用 Playwright 手动验证）、自动保存触发时序（集成测试/手动验证）。

---

## 2. A 类：后端新 Key 契约测试

**文件**：`tests/test_settings_tab_refactor_backend.py`

### 2.1 测试基类

```python
import unittest
from tests._import_app import clear_login_attempts, import_web_app_module


class SettingsTabRefactorBackendTests(unittest.TestCase):
    @classmethod
    def setUpClass(cls):
        cls.module = import_web_app_module()
        cls.app = cls.module.app

    def setUp(self):
        with self.app.app_context():
            clear_login_attempts()
            from outlook_web.repositories import settings as settings_repo
            # 重置新增 key 为默认值
            settings_repo.set_setting("cf_worker_domains", "[]")
            settings_repo.set_setting("cf_worker_default_domain", "")
            settings_repo.set_setting("cf_worker_prefix_rules", '{"min_length":1,"max_length":32,"pattern":"^[a-z0-9][a-z0-9._-]*$"}')

    def _login(self, client, password="testpass123"):
        resp = client.post("/login", json={"password": password})
        self.assertEqual(resp.status_code, 200)
        self.assertTrue(resp.get_json().get("success"))
```

---

### 2.2 测试用例

#### TC-A01：GET /api/settings 返回 3 个新字段 + 默认值

```python
def test_get_settings_returns_cf_worker_defaults(self):
    """GET /api/settings 应包含 3 个 cf_worker_* 字段且默认值正确"""
    client = self.app.test_client()
    self._login(client)

    resp = client.get("/api/settings")
    self.assertEqual(resp.status_code, 200)
    settings = resp.get_json().get("settings", {})

    # 域名列表默认为空列表
    self.assertEqual(settings.get("cf_worker_domains"), [])
    # 默认域名默认为空字符串
    self.assertEqual(settings.get("cf_worker_default_domain"), "")
    # 前缀规则默认值包含 min_length / max_length / pattern
    prefix_rules = settings.get("cf_worker_prefix_rules")
    self.assertIsInstance(prefix_rules, dict)
    self.assertIn("min_length", prefix_rules)
    self.assertIn("max_length", prefix_rules)
    self.assertIn("pattern", prefix_rules)
```

#### TC-A02：PUT 保存 cf_worker_prefix_rules + GET 回环验证

```python
def test_put_cf_worker_prefix_rules_round_trip(self):
    """PUT 保存 cf_worker_prefix_rules 后 GET 能读到"""
    client = self.app.test_client()
    self._login(client)

    new_rules = {"min_length": 3, "max_length": 20, "pattern": "^[a-z][a-z0-9]*$"}
    resp = client.put("/api/settings", json={"cf_worker_prefix_rules": new_rules})
    self.assertEqual(resp.status_code, 200)
    self.assertTrue(resp.get_json().get("success"))

    resp2 = client.get("/api/settings")
    prefix_rules = resp2.get_json().get("settings", {}).get("cf_worker_prefix_rules")
    self.assertEqual(prefix_rules.get("min_length"), 3)
    self.assertEqual(prefix_rules.get("max_length"), 20)
    self.assertEqual(prefix_rules.get("pattern"), "^[a-z][a-z0-9]*$")
```

#### TC-A03：数据隔离 — GPTMail 域名与 CF Worker 域名互不影响

```python
def test_cf_worker_domains_do_not_overwrite_temp_mail_domains(self):
    """cf_worker_domains 和 temp_mail_domains 应存储在独立 key，互不覆盖"""
    client = self.app.test_client()
    self._login(client)

    # 先设置 GPTMail 域名
    gptmail_domains = [{"name": "gptmail.example.com", "enabled": True}]
    client.put("/api/settings", json={"temp_mail_domains": gptmail_domains})

    # 再通过 repository 直接写入 cf_worker_domains（模拟同步操作）
    with self.app.app_context():
        from outlook_web.repositories import settings as settings_repo
        import json
        cf_domains = [{"name": "cf.example.com", "enabled": True}]
        settings_repo.set_setting("cf_worker_domains", json.dumps(cf_domains))

    # 验证两个 key 各自独立
    resp = client.get("/api/settings")
    settings = resp.get_json().get("settings", {})

    temp_domains = settings.get("temp_mail_domains", [])
    cf_domains_result = settings.get("cf_worker_domains", [])

    # GPTMail 域名未被覆盖
    self.assertTrue(any(d.get("name") == "gptmail.example.com" for d in temp_domains),
                    "GPTMail 域名应保持不变")
    # CF Worker 域名独立存在
    self.assertTrue(any(d.get("name") == "cf.example.com" for d in cf_domains_result),
                    "CF Worker 域名应独立存储")
    # 确认互不污染
    self.assertFalse(any(d.get("name") == "cf.example.com" for d in temp_domains),
                     "CF Worker 域名不应出现在 GPTMail 域名列表中")
    self.assertFalse(any(d.get("name") == "gptmail.example.com" for d in cf_domains_result),
                     "GPTMail 域名不应出现在 CF Worker 域名列表中")
```

#### TC-A04：数据隔离 — cf_worker_prefix_rules 与 temp_mail_prefix_rules 独立

```python
def test_cf_worker_prefix_rules_do_not_overwrite_temp_mail_prefix_rules(self):
    """cf_worker_prefix_rules 和 temp_mail_prefix_rules 应独立存储"""
    client = self.app.test_client()
    self._login(client)

    # 设置 GPTMail 前缀规则
    gptmail_rules = {"min_length": 5, "max_length": 30, "pattern": "^[a-z]+$"}
    client.put("/api/settings", json={"temp_mail_prefix_rules": gptmail_rules})

    # 设置 CF Worker 前缀规则
    cf_rules = {"min_length": 2, "max_length": 10, "pattern": "^[a-z0-9]+$"}
    client.put("/api/settings", json={"cf_worker_prefix_rules": cf_rules})

    # 验证两者独立
    resp = client.get("/api/settings")
    settings = resp.get_json().get("settings", {})

    temp_rules = settings.get("temp_mail_prefix_rules", {})
    cf_rules_result = settings.get("cf_worker_prefix_rules", {})

    self.assertEqual(temp_rules.get("min_length"), 5, "GPTMail 前缀规则未变")
    self.assertEqual(cf_rules_result.get("min_length"), 2, "CF Worker 前缀规则独立")
```

#### TC-A05：PUT 不传 cf_worker 字段时，cf_worker_domains 保持不变

```python
def test_put_other_fields_does_not_clear_cf_worker_domains(self):
    """PUT 时不传 cf_worker_domains，已有的域名应保持不变"""
    with self.app.app_context():
        from outlook_web.repositories import settings as settings_repo
        import json
        cf_domains = [{"name": "preserved.example.com", "enabled": True}]
        settings_repo.set_setting("cf_worker_domains", json.dumps(cf_domains))

    client = self.app.test_client()
    self._login(client)

    # PUT 只修改其他字段
    resp = client.put("/api/settings", json={"cf_worker_prefix_rules": {"min_length": 3, "max_length": 20}})
    self.assertTrue(resp.get_json().get("success"))

    # cf_worker_domains 应保持不变
    resp2 = client.get("/api/settings")
    domains = resp2.get_json().get("settings", {}).get("cf_worker_domains", [])
    self.assertTrue(any(d.get("name") == "preserved.example.com" for d in domains))
```

#### TC-A06：DB 默认值 — 新 key 初始化后存在

```python
def test_db_default_values_exist_after_init(self):
    """数据库初始化后 3 个 cf_worker_* key 应存在且有默认值"""
    with self.app.app_context():
        from outlook_web.repositories import settings as settings_repo
        import json

        domains_raw = settings_repo.get_setting("cf_worker_domains", None)
        default_domain_raw = settings_repo.get_setting("cf_worker_default_domain", None)
        prefix_rules_raw = settings_repo.get_setting("cf_worker_prefix_rules", None)

        self.assertIsNotNone(domains_raw, "cf_worker_domains 应有默认值")
        self.assertIsNotNone(default_domain_raw, "cf_worker_default_domain 应有默认值")
        self.assertIsNotNone(prefix_rules_raw, "cf_worker_prefix_rules 应有默认值")

        # 默认值可正确解析
        domains = json.loads(domains_raw)
        self.assertEqual(domains, [])

        prefix_rules = json.loads(prefix_rules_raw)
        self.assertIn("min_length", prefix_rules)
```

#### TC-A07：Repository getter 函数 — get_cf_worker_domains 返回 list

```python
def test_get_cf_worker_domains_returns_list(self):
    """get_cf_worker_domains() 应返回 list 类型"""
    with self.app.app_context():
        from outlook_web.repositories import settings as settings_repo

        result = settings_repo.get_cf_worker_domains()
        self.assertIsInstance(result, list)
```

#### TC-A08：Repository getter 函数 — get_cf_worker_domains 损坏 JSON 时返回空列表

```python
def test_get_cf_worker_domains_handles_invalid_json(self):
    """cf_worker_domains 存储了非法 JSON 时，getter 应返回空列表而不是抛异常"""
    with self.app.app_context():
        from outlook_web.repositories import settings as settings_repo

        settings_repo.set_setting("cf_worker_domains", "not-valid-json", commit=True)
        result = settings_repo.get_cf_worker_domains()
        self.assertEqual(result, [])
```

#### TC-A09：Repository getter 函数 — get_cf_worker_prefix_rules 损坏 JSON 时返回默认值

```python
def test_get_cf_worker_prefix_rules_handles_invalid_json(self):
    """cf_worker_prefix_rules 存储了非法 JSON 时，getter 应返回带默认键的 dict"""
    with self.app.app_context():
        from outlook_web.repositories import settings as settings_repo

        settings_repo.set_setting("cf_worker_prefix_rules", "{{broken", commit=True)
        result = settings_repo.get_cf_worker_prefix_rules()
        self.assertIsInstance(result, dict)
        self.assertIn("min_length", result)
        self.assertIn("max_length", result)
        self.assertIn("pattern", result)
```

#### TC-A10：同步接口写入 cf_worker_* 而非 temp_mail_*

```python
def test_sync_cf_worker_writes_to_cf_worker_keys_not_temp_mail(self):
    """
    api_sync_cf_worker_domains 应写入 cf_worker_domains / cf_worker_default_domain，
    不应覆盖 temp_mail_domains / temp_mail_default_domain。
    使用 mock 模拟 CF Worker 响应。
    """
    from unittest.mock import patch
    import json

    # 预设 GPTMail 域名（用于验证不被覆盖）
    with self.app.app_context():
        from outlook_web.repositories import settings as settings_repo
        original_gptmail_domains = [{"name": "gptmail.example.com", "enabled": True}]
        settings_repo.set_setting("temp_mail_domains", json.dumps(original_gptmail_domains))
        settings_repo.set_setting("cf_worker_base_url", "https://worker.example.com")
        settings_repo.set_setting("cf_worker_admin_key", "test-admin-key")

    client = self.app.test_client()
    self._login(client)

    # Mock CF Worker 的 Admin API 响应
    mock_domains_response = {
        "domains": ["cf1.example.com", "cf2.example.com"],
        "default_domain": "cf1.example.com",
    }

    with patch("outlook_web.controllers.settings._fetch_cf_worker_domains",
               return_value=mock_domains_response) as _mock:
        resp = client.post("/api/settings/cf-worker-sync-domains")
        # 实际路由路径：/api/settings/cf-worker-sync-domains（已确认）

    if resp.status_code == 200:
        # 验证 cf_worker_* key 已更新
        with self.app.app_context():
            from outlook_web.repositories import settings as settings_repo
            cf_domains = settings_repo.get_cf_worker_domains()
            cf_default = settings_repo.get_cf_worker_default_domain()

            self.assertTrue(any(d.get("name") in ["cf1.example.com", "cf2.example.com"]
                                for d in cf_domains),
                            "cf_worker_domains 应包含同步到的域名")
            self.assertEqual(cf_default, "cf1.example.com")

        # 验证 temp_mail_domains 未被覆盖
        resp2 = client.get("/api/settings")
        temp_domains = resp2.get_json().get("settings", {}).get("temp_mail_domains", [])
        self.assertTrue(any(d.get("name") == "gptmail.example.com" for d in temp_domains),
                        "temp_mail_domains 不应被同步覆盖")
    else:
        # 如果同步接口 mock 路径不对，此 case 标记为跳过
        self.skipTest(f"同步接口 mock 路径需按实际调整，当前状态码：{resp.status_code}")
```

#### TC-A11：PUT cf_worker_prefix_rules 格式校验 — 非 dict 拒绝

```python
def test_put_cf_worker_prefix_rules_rejects_non_dict(self):
    """cf_worker_prefix_rules 传入非 dict 值应返回错误"""
    client = self.app.test_client()
    self._login(client)

    resp = client.put("/api/settings", json={"cf_worker_prefix_rules": "not-a-dict"})
    data = resp.get_json()
    # 应该报错或 errors 列表非空
    errors = data.get("errors", [])
    self.assertTrue(len(errors) > 0 or not data.get("success"),
                    "非 dict 的 cf_worker_prefix_rules 应返回错误")
```

#### TC-A12：PUT 只传 cf_worker 字段，temp_mail 字段保持不变

```python
def test_put_cf_worker_fields_only_does_not_touch_temp_mail(self):
    """只保存 CF Worker 相关字段时，temp_mail 相关字段保持不变"""
    client = self.app.test_client()
    self._login(client)

    # 先设置 temp_mail 相关字段
    original_url = "https://gptmail.original.com"
    client.put("/api/settings", json={"temp_mail_api_base_url": original_url})

    # 只更新 CF Worker 字段
    resp = client.put("/api/settings", json={
        "cf_worker_base_url": "https://worker.example.com",
        "cf_worker_prefix_rules": {"min_length": 2, "max_length": 15},
    })
    self.assertTrue(resp.get_json().get("success"))

    # temp_mail 字段不变
    resp2 = client.get("/api/settings")
    settings = resp2.get_json().get("settings", {})
    self.assertEqual(settings.get("temp_mail_api_base_url"), original_url)
```

#### TC-A13：GET 包含 temp_mail_provider 字段

```python
def test_get_settings_includes_temp_mail_provider(self):
    """GET /api/settings 应包含 temp_mail_provider 字段"""
    client = self.app.test_client()
    self._login(client)

    resp = client.get("/api/settings")
    settings = resp.get_json().get("settings", {})

    self.assertIn("temp_mail_provider", settings,
                  "settings 应包含 temp_mail_provider 字段")
```

#### TC-A14：未登录时 GET 返回认证错误

```python
def test_get_settings_requires_auth(self):
    """未登录时 GET /api/settings 应返回认证错误"""
    client = self.app.test_client()
    resp = client.get("/api/settings")
    self.assertNotEqual(resp.status_code, 200)
```

---

## 3. B 类：前端契约测试

**文件**：`tests/test_settings_tab_refactor_frontend.py`

### 3.1 测试基类

```python
import unittest
from tests._import_app import import_web_app_module


class SettingsTabRefactorFrontendTests(unittest.TestCase):
    @classmethod
    def setUpClass(cls):
        cls.module = import_web_app_module()
        cls.app = cls.module.app

    def _login(self, client, password="testpass123"):
        resp = client.post("/login", json={"password": password})
        self.assertEqual(resp.status_code, 200)
        self.assertTrue(resp.get_json().get("success"))

    def _get_text(self, client, path="/"):
        resp = client.get(path)
        try:
            return resp.data.decode("utf-8")
        finally:
            resp.close()
```

---

### 3.2 测试用例

#### TC-B01：index.html 包含 4 个 Tab 按钮

```python
def test_index_html_contains_four_settings_tabs(self):
    """index.html 应包含 4 个 settings-tab 按钮"""
    client = self.app.test_client()
    self._login(client)
    html = self._get_text(client)

    # 检查 Tab 导航栏存在
    self.assertIn('class="settings-tab-nav"', html,
                  "应包含 .settings-tab-nav 导航栏")

    # 检查 4 个 Tab 按钮的 data-tab 属性
    self.assertIn('data-tab="basic"', html, "应包含基础 Tab")
    self.assertIn('data-tab="temp-mail"', html, "应包含临时邮箱 Tab")
    self.assertIn('data-tab="api-security"', html, "应包含 API 安全 Tab")
    self.assertIn('data-tab="automation"', html, "应包含自动化 Tab")
```

#### TC-B02：index.html 包含 4 个 Tab 面板

```python
def test_index_html_contains_four_tab_panes(self):
    """index.html 应包含 4 个 settings-tab-pane 内容面板"""
    client = self.app.test_client()
    self._login(client)
    html = self._get_text(client)

    self.assertIn('id="settings-tab-basic"', html)
    self.assertIn('id="settings-tab-temp-mail"', html)
    self.assertIn('id="settings-tab-api-security"', html)
    self.assertIn('id="settings-tab-automation"', html)
```

#### TC-B03：index.html 包含 Provider 单选按钮组

```python
def test_index_html_contains_provider_radio_buttons(self):
    """index.html 应包含 Provider 单选按钮（非下拉框）"""
    client = self.app.test_client()
    self._login(client)
    html = self._get_text(client)

    # 存在 radio 类型的 Provider 选择
    self.assertIn('name="tempMailProvider"', html,
                  "应包含 tempMailProvider 单选按钮组")
    self.assertIn('value="legacy_bridge"', html,
                  "应包含 legacy_bridge 选项")
    self.assertIn('value="cloudflare_temp_mail"', html,
                  "应包含 cloudflare_temp_mail 选项")

    # 不应再有 Provider 下拉框（旧的 select 元素）
    # 检查没有旧的 settingsTempMailProvider select
    self.assertNotIn('<select id="settingsTempMailProvider"', html,
                     "不应保留旧的 Provider <select> 元素")
```

#### TC-B04：index.html 包含 GPTMail 配置面板

```python
def test_index_html_contains_gptmail_config_panel(self):
    """index.html 应包含 GPTMail 配置面板及其字段"""
    client = self.app.test_client()
    self._login(client)
    html = self._get_text(client)

    self.assertIn('id="gptmailConfigPanel"', html, "应包含 GPTMail 配置面板")
    self.assertIn('id="settingsTempMailApiBaseUrl"', html)
    self.assertIn('id="settingsTempMailApiKey"', html)
    self.assertIn('id="settingsTempMailDomains"', html)
    self.assertIn('id="settingsTempMailDefaultDomain"', html)
    self.assertIn('id="settingsTempMailPrefixRules"', html)
```

#### TC-B05：index.html 包含 CF Worker 配置面板及只读字段

```python
def test_index_html_contains_cf_worker_config_panel(self):
    """index.html 应包含 CF Worker 配置面板及只读域名字段"""
    client = self.app.test_client()
    self._login(client)
    html = self._get_text(client)

    self.assertIn('id="cfWorkerConfigPanel"', html, "应包含 CF Worker 配置面板")
    self.assertIn('id="settingsCfWorkerBaseUrl"', html)
    self.assertIn('id="settingsCfWorkerAdminKey"', html)
    self.assertIn('id="settingsCfWorkerDomains"', html)
    self.assertIn('id="settingsCfWorkerDefaultDomain"', html)
    self.assertIn('id="settingsCfWorkerPrefixRules"', html)
    self.assertIn('id="cfWorkerSyncTime"', html)
```

#### TC-B06：index.html 中 CF Worker 只读字段有 readonly 属性

```python
def test_cf_worker_domain_fields_have_readonly_attribute(self):
    """CF Worker 域名字段应有 readonly HTML 属性"""
    client = self.app.test_client()
    self._login(client)
    html = self._get_text(client)

    # 简单检查 readonly 属性存在于这两个字段的相邻 HTML 中
    # 更精确的检查可用 BeautifulSoup 解析 DOM
    self.assertIn('id="settingsCfWorkerDomains"', html)
    # 检查该 textarea/input 有 readonly
    # 通过查找包含该 id 的行或片段里是否有 readonly
    idx = html.find('id="settingsCfWorkerDomains"')
    surrounding = html[max(0, idx - 10):idx + 200]
    self.assertIn("readonly", surrounding,
                  "settingsCfWorkerDomains 应有 readonly 属性")
```

#### TC-B07：main.js 包含 switchSettingsTab 函数

```python
def test_main_js_contains_switch_settings_tab_function(self):
    """main.js 应包含 switchSettingsTab 函数定义"""
    client = self.app.test_client()
    resp = client.get("/static/js/main.js")
    js_text = resp.data.decode("utf-8")
    self.assertIn("function switchSettingsTab", js_text,
                  "main.js 应包含 switchSettingsTab 函数")
```

#### TC-B08：main.js 包含 onTempMailProviderChange 函数

```python
def test_main_js_contains_on_temp_mail_provider_change(self):
    """main.js 应包含 onTempMailProviderChange 函数定义"""
    client = self.app.test_client()
    resp = client.get("/static/js/main.js")
    js_text = resp.data.decode("utf-8")
    self.assertIn("function onTempMailProviderChange", js_text,
                  "main.js 应包含 onTempMailProviderChange 函数")
```

#### TC-B09：main.js 包含 autoSaveSettings 函数

```python
def test_main_js_contains_auto_save_settings_function(self):
    """main.js 应包含 autoSaveSettings 函数定义"""
    client = self.app.test_client()
    resp = client.get("/static/js/main.js")
    js_text = resp.data.decode("utf-8")
    self.assertIn("function autoSaveSettings", js_text,
                  "main.js 应包含 autoSaveSettings 函数")
```

#### TC-B10：main.css 包含 Tab 相关样式类

```python
def test_main_css_contains_tab_styles(self):
    """main.css 应包含 .settings-tab-nav 和 .settings-tab 样式"""
    client = self.app.test_client()
    resp = client.get("/static/css/main.css")
    css_text = resp.data.decode("utf-8")

    self.assertIn(".settings-tab-nav", css_text,
                  "main.css 应包含 .settings-tab-nav 样式")
    self.assertIn(".settings-tab", css_text,
                  "main.css 应包含 .settings-tab 样式")
    self.assertIn(".settings-tab-pane", css_text,
                  "main.css 应包含 .settings-tab-pane 样式")
```

#### TC-B11：main.css 包含 Provider 单选按钮样式

```python
def test_main_css_contains_provider_radio_styles(self):
    """main.css 应包含 .provider-radio-group 和 .provider-radio 样式"""
    client = self.app.test_client()
    resp = client.get("/static/css/main.css")
    css_text = resp.data.decode("utf-8")

    self.assertIn(".provider-radio-group", css_text,
                  "main.css 应包含 .provider-radio-group 样式")
    self.assertIn(".provider-radio", css_text,
                  "main.css 应包含 .provider-radio 样式")
```

#### TC-B12：main.css 包含只读字段样式

```python
def test_main_css_contains_readonly_field_styles(self):
    """main.css 应包含 .readonly-field 样式"""
    client = self.app.test_client()
    resp = client.get("/static/css/main.css")
    css_text = resp.data.decode("utf-8")

    self.assertIn(".readonly-field", css_text,
                  "main.css 应包含 .readonly-field 样式")
```

---

## 4. 执行说明

### 4.1 执行命令

```bash
# A 类：后端测试
python -m pytest tests/test_settings_tab_refactor_backend.py -v

# B 类：前端契约测试
python -m pytest tests/test_settings_tab_refactor_frontend.py -v

# 全部运行
python -m pytest tests/test_settings_tab_refactor_backend.py tests/test_settings_tab_refactor_frontend.py -v
```

### 4.2 测试前置条件

- 测试使用 in-memory 测试数据库（隔离），不影响实际数据
- 默认密码 `testpass123` 与测试环境配置一致
- TC-A10（同步接口测试）使用 `unittest.mock.patch`，需确认 mock 路径与实际 controller 函数路径一致

### 4.3 TDD 执行策略

按照 TDD 实践，建议**先写测试，再写实现**：

1. 先创建两个测试文件（此时所有测试均失败）
2. 实现后端改动（`db.py` → `repository` → `controller`）
3. 运行 A 类测试，确认全部通过
4. 实现前端改动（`index.html` → `main.css` → `main.js`）
5. 运行 B 类测试，确认全部通过
6. 手动验证 Tab 切换交互、自动保存、Provider 切换、CF Worker 同步

---

## 5. 用例覆盖矩阵

| 验收标准（来自 PRD §10） | 覆盖用例 |
|------|------|
| GET 接口返回新字段 | TC-A01 |
| PUT 保存新字段 | TC-A02, TC-A12 |
| 数据隔离（GPTMail vs CF Worker 域名） | TC-A03 |
| 数据隔离（前缀规则） | TC-A04 |
| 只 PUT cf_worker 字段不影响 temp_mail | TC-A05, TC-A12 |
| DB 默认值初始化 | TC-A06 |
| Repository getter 健壮性 | TC-A07, TC-A08, TC-A09 |
| 同步接口写入正确 key | TC-A10 |
| 格式校验 | TC-A11 |
| temp_mail_provider 字段存在 | TC-A13 |
| 认证保护 | TC-A14 |
| HTML Tab 导航结构 | TC-B01, TC-B02 |
| Provider radio button（非 select） | TC-B03 |
| GPTMail 配置面板字段 | TC-B04 |
| CF Worker 配置面板字段 | TC-B05 |
| 只读字段 HTML 属性 | TC-B06 |
| JS 函数存在性 | TC-B07, TC-B08, TC-B09 |
| CSS 样式类存在性 | TC-B10, TC-B11, TC-B12 |

---

*记录人：AI 助手 | 2026-04-04*
