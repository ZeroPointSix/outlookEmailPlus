# FD: 版本更新检测与一键更新

- 文档版本: v0.1
- 创建日期: 2026-04-06
- 关联 PRD: `docs/PRD/2026-04-06-版本更新检测与一键更新-PRD.md` (v0.1)
- 当前范围: 版本检测 API、一键更新 API、前端 UI 交互流程、各文件修改明细、docker-compose 配置

---

## 版本变更记录

| 版本 | 日期 | 变更说明 |
|------|------|------|
| v0.1 | 2026-04-06 | 初稿 |

---

## 0. 当前实施状态

**设计完成，待代码实现。**

---

## 1. 总体架构

### 1.1 数据流

```
[浏览器页面加载]
    │
    ▼
GET /api/system/version-check
    │  ← 服务端：内存缓存版本比较结果（TTL 10 分钟）
    │  ← 首次缓存未命中时调用 GitHub API 获取最新 Release
    │
    ▼
返回 { has_update, current_version, latest_version, release_url }
    │
    ├─ has_update = false → 不显示任何提示，静默结束
    │
    └─ has_update = true → 顶部显示横幅提示
                               │
                               ▼
                          用户点击"立即更新"
                               │
                               ▼
                     POST /api/system/trigger-update
                               │  ← 服务端：调用 Watchtower HTTP API
                               │             Authorization: Bearer <token>
                               │             POST http://watchtower:8080/v1/update
                               │
                               ▼
                     返回 { success: true, message: "更新触发成功" }
                               │
                               ▼
                     前端开始轮询 /healthz（每 3 秒，最长 90 秒）
                               │
                               ├─ 检测到恢复 → 弹出 Toast "更新完成，页面已刷新" → 刷新页面
                               └─ 90 秒超时 → 弹出 Toast "更新超时，请手动检查容器状态"
```

### 1.2 组件职责划分

| 组件 | 职责 |
|------|------|
| `GET /api/system/version-check` | 版本比较；内存缓存；调 GitHub API |
| `POST /api/system/trigger-update` | 调 Watchtower API 触发更新；仅传递触发信号 |
| `GET /healthz` | 轮询探活；无需鉴权 |
| 前端 `checkVersionUpdate()` | 页面加载时调一次 version-check；渲染横幅 |
| 前端 `triggerUpdate()` | 点击更新按钮时调 trigger-update |
| 前端 `waitForRestart()` | 轮询 /healthz，处理重启等待逻辑 |

---

## 2. 后端：版本检测 API

### 2.1 路由注册

**文件：** `outlook_web/routes/system.py`

新增：

```python
bp.add_url_rule(
    "/api/system/version-check",
    view_func=system_controller.api_version_check,
    methods=["GET"],
)
```

### 2.2 控制器 `api_version_check()`

**文件：** `outlook_web/controllers/system.py`

#### 2.2.1 模块级缓存变量

```python
# 模块顶部，版本检测内存缓存（重启后清空）
_version_cache: dict | None = None
_version_cache_at: float = 0.0
_VERSION_CACHE_TTL = 600  # 10 分钟
```

#### 2.2.2 函数逻辑

```python
@login_required
def api_version_check() -> Any:
    """检查是否有新版本可用（内存缓存，10 分钟 TTL）"""
    import time, urllib.request, json as _json

    global _version_cache, _version_cache_at

    now = time.time()
    if _version_cache is not None and (now - _version_cache_at) < _VERSION_CACHE_TTL:
        return jsonify(_version_cache)

    current = APP_VERSION  # 来自 outlook_web.__version__

    try:
        GITHUB_API = "https://api.github.com/repos/hshaokang/outlookemail-plus/releases/latest"
        req = urllib.request.Request(GITHUB_API, headers={"User-Agent": "outlook-email-plus"})
        with urllib.request.urlopen(req, timeout=5) as resp:
            data = _json.loads(resp.read())
        latest = data.get("tag_name", "").lstrip("v")
        release_url = data.get("html_url", "")
        has_update = _version_gt(latest, current)
        result = {
            "success": True,
            "has_update": has_update,
            "current_version": current,
            "latest_version": latest,
            "release_url": release_url,
        }
    except Exception:
        # GitHub API 调用失败：静默降级，返回无更新
        result = {
            "success": True,
            "has_update": False,
            "current_version": current,
            "latest_version": current,
            "release_url": "",
        }

    _version_cache = result
    _version_cache_at = now
    return jsonify(result)
```

#### 2.2.3 版本比较函数 `_version_gt()`

```python
def _version_gt(a: str, b: str) -> bool:
    """判断版本 a 是否严格大于版本 b（仅支持语义化版本 x.y.z）"""
    try:
        return tuple(int(x) for x in a.split(".")) > tuple(int(x) for x in b.split("."))
    except Exception:
        return False
```

#### 2.2.4 错误边界

| 场景 | 处理方式 |
|------|------|
| GitHub API 超时（>5s） | 静默降级，返回 `has_update: false` |
| GitHub API 返回非 200 | 静默降级 |
| `tag_name` 格式异常 | `_version_gt()` 捕获异常返回 `false` |
| 缓存命中 | 直接返回缓存，不请求 GitHub |

---

## 3. 后端：一键更新 API

### 3.1 路由注册

**文件：** `outlook_web/routes/system.py`

新增：

```python
bp.add_url_rule(
    "/api/system/trigger-update",
    view_func=system_controller.api_trigger_update,
    methods=["POST"],
)
```

### 3.2 控制器 `api_trigger_update()`

**文件：** `outlook_web/controllers/system.py`

```python
@login_required
def api_trigger_update() -> Any:
    """触发 Watchtower 一键更新（调用 Watchtower HTTP API）"""
    import urllib.request, urllib.error, os

    watchtower_url = os.getenv("WATCHTOWER_API_URL", "http://watchtower:8080")
    watchtower_token = os.getenv("WATCHTOWER_HTTP_API_TOKEN", "")

    if not watchtower_token:
        return jsonify({"success": False, "message": "WATCHTOWER_HTTP_API_TOKEN 未配置"}), 500

    try:
        req = urllib.request.Request(
            f"{watchtower_url}/v1/update",
            method="POST",
            headers={
                "Authorization": f"Bearer {watchtower_token}",
                "Content-Length": "0",
            },
            data=b"",
        )
        with urllib.request.urlopen(req, timeout=10) as resp:
            status = resp.status
        if status == 200:
            return jsonify({"success": True, "message": "更新触发成功，容器即将重启"})
        else:
            return jsonify({"success": False, "message": f"Watchtower 返回状态码 {status}"}), 502
    except urllib.error.URLError as e:
        return jsonify({"success": False, "message": f"无法连接 Watchtower: {e.reason}"}), 503
    except Exception as e:
        return jsonify({"success": False, "message": f"触发更新失败: {str(e)}"}), 500
```

### 3.3 环境变量

| 环境变量 | 说明 | 默认值 |
|------|------|------|
| `WATCHTOWER_API_URL` | Watchtower 服务地址 | `http://watchtower:8080` |
| `WATCHTOWER_HTTP_API_TOKEN` | Watchtower 认证 Token（必须配置） | 空（空时接口返回 500） |

### 3.4 错误边界

| 场景 | HTTP 状态 | 返回 message |
|------|------|------|
| Token 未配置 | 500 | `WATCHTOWER_HTTP_API_TOKEN 未配置` |
| Watchtower 连接失败 | 503 | `无法连接 Watchtower: ...` |
| Watchtower 返回非 200 | 502 | `Watchtower 返回状态码 xxx` |
| 其他异常 | 500 | `触发更新失败: ...` |
| 成功 | 200 | `更新触发成功，容器即将重启` |

---

## 4. 前端

### 4.1 版本检测横幅 UI

**文件：** `templates/index.html`

在 `<body>` 开头或顶部导航栏下方插入：

```html
<!-- 版本更新提示横幅（默认隐藏） -->
<div id="versionUpdateBanner" class="version-update-banner d-none">
  <span id="versionUpdateMsg"></span>
  <button id="btnTriggerUpdate" class="btn btn-sm btn-warning ms-3" onclick="triggerUpdate()">
    立即更新
  </button>
  <button class="btn btn-sm btn-link ms-1" onclick="dismissVersionBanner()">忽略</button>
</div>
```

### 4.2 CSS 样式（inline 或 style.css）

```css
.version-update-banner {
  background: #fff3cd;
  border-bottom: 1px solid #ffc107;
  padding: 8px 16px;
  display: flex;
  align-items: center;
  font-size: 14px;
  z-index: 1000;
}
```

### 4.3 JavaScript 函数

**文件：** `static/js/main.js`

#### 4.3.1 `checkVersionUpdate()`

```javascript
/**
 * 页面加载时调用一次，检查是否有可用更新
 */
async function checkVersionUpdate() {
  try {
    const res = await fetch('/api/system/version-check');
    if (!res.ok) return;
    const data = await res.json();
    if (data.has_update) {
      const banner = document.getElementById('versionUpdateBanner');
      const msg = document.getElementById('versionUpdateMsg');
      msg.innerHTML = `发现新版本 <strong>v${data.latest_version}</strong>（当前 v${data.current_version}）
        <a href="${data.release_url}" target="_blank" class="ms-1">查看更新日志</a>`;
      banner.classList.remove('d-none');
    }
  } catch (e) {
    // 静默失败
  }
}

function dismissVersionBanner() {
  document.getElementById('versionUpdateBanner').classList.add('d-none');
}
```

#### 4.3.2 `triggerUpdate()`

```javascript
/**
 * 用户点击"立即更新"时触发
 */
async function triggerUpdate() {
  const btn = document.getElementById('btnTriggerUpdate');
  btn.disabled = true;
  btn.textContent = '正在触发更新...';

  try {
    const res = await fetch('/api/system/trigger-update', {
      method: 'POST',
      headers: { 'X-CSRFToken': getCSRFToken() },
    });
    const data = await res.json();
    if (data.success) {
      btn.textContent = '等待容器重启...';
      await waitForRestart();
    } else {
      showToast('更新失败：' + (data.message || '未知错误'), 'error');
      btn.disabled = false;
      btn.textContent = '立即更新';
    }
  } catch (e) {
    showToast('更新请求失败，请检查网络', 'error');
    btn.disabled = false;
    btn.textContent = '立即更新';
  }
}
```

#### 4.3.3 `waitForRestart()`

```javascript
/**
 * 轮询 /healthz 等待容器重启后恢复
 * - 立即开始轮询，每 3 秒一次
 * - 最长等待 90 秒，超时提示用户手动检查
 * - 检测到服务恢复后刷新页面
 */
async function waitForRestart() {
  const MAX_WAIT_MS = 90000;  // 90 秒
  const POLL_INTERVAL_MS = 3000;  // 每 3 秒
  const startAt = Date.now();

  while (Date.now() - startAt < MAX_WAIT_MS) {
    await new Promise(resolve => setTimeout(resolve, POLL_INTERVAL_MS));
    try {
      const res = await fetch('/healthz', { cache: 'no-store' });
      if (res.ok) {
        showToast('更新完成，正在刷新页面...', 'success');
        setTimeout(() => location.reload(), 1500);
        return;
      }
    } catch (e) {
      // 容器正在重启中，继续轮询
    }
  }

  // 超时处理
  showToast('更新超时，请手动检查容器状态', 'warning', 8000);
  const btn = document.getElementById('btnTriggerUpdate');
  if (btn) {
    btn.disabled = false;
    btn.textContent = '立即更新';
  }
}
```

### 4.4 页面加载时调用

**文件：** `static/js/main.js`，在页面初始化函数中追加调用：

```javascript
// 在 DOMContentLoaded 或现有的 init 函数中添加
checkVersionUpdate();
```

### 4.5 CSRF Token 辅助函数

若项目中已存在 `getCSRFToken()` 函数则直接复用；若不存在，新增：

```javascript
function getCSRFToken() {
  const meta = document.querySelector('meta[name="csrf-token"]');
  return meta ? meta.getAttribute('content') : '';
}
```

---

## 5. i18n 翻译

**文件：** `static/js/i18n.js`

新增以下翻译键（中英文）：

```javascript
// 版本更新相关
"发现新版本": { en: "New version available" },
"查看更新日志": { en: "View changelog" },
"立即更新": { en: "Update now" },
"忽略": { en: "Dismiss" },
"正在触发更新...": { en: "Triggering update..." },
"等待容器重启...": { en: "Waiting for restart..." },
"更新完成，正在刷新页面...": { en: "Update complete, reloading..." },
"更新超时，请手动检查容器状态": { en: "Update timed out, please check container status" },
"更新失败：": { en: "Update failed: " },
"更新请求失败，请检查网络": { en: "Update request failed, check network" },
```

---

## 6. Docker Compose 配置（合并到现有文件）

**文件：** `docker-compose.yml`（在现有文件中新增 `watchtower` 服务 + 给 `app` 服务打标签 + 新增环境变量）

**需要做的修改：**

1. 在 `app` 服务下新增：
   - 环境变量 `WATCHTOWER_HTTP_API_TOKEN`
   - label `com.centurylinklabs.watchtower.enable=true`
   - 加入 `outlook-net` network

2. 新增 `watchtower` 服务：

```yaml
  watchtower:
    image: containrrr/watchtower
    container_name: watchtower
    restart: unless-stopped
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - WATCHTOWER_HTTP_API_TOKEN=${WATCHTOWER_HTTP_API_TOKEN}
      - WATCHTOWER_HTTP_API_UPDATE=true
      - WATCHTOWER_CLEANUP=true
      # 禁用定时轮询，仅通过 HTTP API 触发
      - WATCHTOWER_HTTP_API_PERIODIC_POLLS=false
    command: --http-api-update --label-enable
    labels:
      - "com.centurylinklabs.watchtower.enable=false"
    networks:
      - outlook-net
```

3. 在 `networks` 下新增（若不存在）：

```yaml
networks:
  outlook-net:
    driver: bridge
```

> **说明：**
> - `WATCHTOWER_HTTP_API_TOKEN` 必须在 `.env` 文件中配置，且 app 与 watchtower 两个服务使用同一个 token。
> - `--label-enable` 确保 watchtower 只更新带有 `com.centurylinklabs.watchtower.enable=true` 标签的容器（即 `app` 服务），不更新 watchtower 自身。
> - Watchtower 默认监听 `0.0.0.0:8080`，在同一 Docker network 内，app 通过 `http://watchtower:8080` 访问。

---

## 7. 各文件修改明细

| 文件 | 类型 | 变更说明 |
|------|------|------|
| `outlook_web/controllers/system.py` | 修改 | 新增模块级缓存变量 + `api_version_check()` + `api_trigger_update()` + `_version_gt()` |
| `outlook_web/routes/system.py` | 修改 | 注册 `GET /api/system/version-check` 和 `POST /api/system/trigger-update` |
| `templates/index.html` | 修改 | 新增 `#versionUpdateBanner` 横幅 HTML |
| `static/js/main.js` | 修改 | 新增 `checkVersionUpdate()`、`dismissVersionBanner()`、`triggerUpdate()`、`waitForRestart()` |
| `static/js/i18n.js` | 修改 | 新增版本更新相关翻译键 |
| `docker-compose.yml` | 修改 | 新增 watchtower 服务；app 服务新增环境变量和标签；新增 outlook-net network |

---

## 8. 边界条件与注意事项

### 8.1 GitHub API 限制

- GitHub Public API 对匿名请求限速为 60 次/小时/IP
- 服务端缓存 TTL 设为 10 分钟，单实例场景下每小时最多 6 次请求，不会触发限速
- 调用失败时静默降级（返回 `has_update: false`），不影响用户正常使用

### 8.2 Watchtower Token 安全

- Token 通过环境变量传入，不写入代码
- 建议使用 `python -c "import secrets; print(secrets.token_hex(32))"` 生成高强度 token

### 8.3 更新期间的服务中断

- 更新过程中，容器会短暂停止（约 10~30 秒）
- 轮询 `/healthz` 会连续出现连接失败，这是预期行为
- `fetch('/healthz')` 抛出的网络异常被 `catch` 捕获后继续轮询，不弹错误

### 8.4 非 Docker 环境

- 若用户未部署 Watchtower（`WATCHTOWER_HTTP_API_TOKEN` 为空），`trigger-update` 接口返回 500 提示配置缺失
- 版本检测功能不依赖 Watchtower，在所有环境下均可正常工作

### 8.5 CSRF 保护

- `POST /api/system/trigger-update` 受 flask-wtf CSRF 保护
- 前端使用 `getCSRFToken()` 从 `<meta name="csrf-token">` 读取 token，附在请求头

---

## 9. 开放问题

| # | 问题 | 状态 |
|---|------|------|
| 1 | GitHub repo 名称是否确认为 `hshaokang/outlookemail-plus`？ | ✅ 已确认 |
| 2 | `docker-compose.prod.yml` 是否与现有 `docker-compose.yml` 合并，还是独立生产文件？ | ✅ 已确认（合并到现有文件） |
| 3 | 现有 `templates/index.html` 中是否已有 `<meta name="csrf-token">` 标签？ | ✅ 已确认（已有，直接复用） |
