# TD: 设置页面整体 UI 重构 — Tab 化 + 临时邮箱配置区分离

- 文档版本: v0.2
- 创建日期: 2026-04-04
- 关联 PRD: `docs/PRD/2026-04-04-设置页面UI重构-临时邮箱配置区分离PRD.md` (v0.4)
- 关联 TODO: `docs/TODO/2026-04-03-设置页面UI重构-临时邮箱配置区分离TODO.md`

---

## 1. 文件变更清单

| 文件 | 变更类型 | 预估改动量 | 说明 |
|------|---------|----------|------|
| `templates/index.html` | 重构 | ~200 行 | 设置页面 HTML 结构重写，引入 4-Tab 导航，临时邮箱区分离 |
| `static/js/main.js` | 重构 | ~150 行 | Tab 切换逻辑、自动保存、Provider 面板显隐、只读状态管理 |
| `static/css/main.css` | 小改 | ~50 行 | Tab 导航样式、只读字段样式、去除 720px 宽度限制 |
| `outlook_web/db.py` | 新增 | ~3 行 | 新增 3 个 settings key 默认值 |
| `outlook_web/repositories/settings.py` | 新增 | ~30 行 | 新增 3 个 getter 函数 |
| `outlook_web/controllers/settings.py` | 修改 | ~20 行 | GET 暴露新 key + PUT 处理新 key + 同步接口改写目标 key |

---

## 2. 后端实现

### 2.1 `outlook_web/db.py` — 新增 settings 默认值

**位置**：约 647 行（temp_mail_prefix_rules 之后）

```python
# v0.3: 设置页面 Tab 重构 — CF Worker 独立域名 key
cursor.execute("""
    INSERT OR IGNORE INTO settings (key, value)
    VALUES ('cf_worker_domains', '[]')
""")
cursor.execute("""
    INSERT OR IGNORE INTO settings (key, value)
    VALUES ('cf_worker_default_domain', '')
""")
cursor.execute("""
    INSERT OR IGNORE INTO settings (key, value)
    VALUES ('cf_worker_prefix_rules', '{"min_length":1,"max_length":32,"pattern":"^[a-z0-9][a-z0-9._-]*$"}')
""")
```

### 2.2 `outlook_web/repositories/settings.py` — 新增 getter 函数

**位置**：约 151 行（get_temp_mail_prefix_rules 之后）

```python
def get_cf_worker_domains() -> list[dict[str, Any]]:
    raw = get_setting("cf_worker_domains", "[]")
    try:
        return json.loads(raw)
    except Exception:
        return []

def get_cf_worker_default_domain() -> str:
    return get_setting("cf_worker_default_domain", "").strip()

def get_cf_worker_prefix_rules() -> dict[str, Any]:
    raw = get_setting("cf_worker_prefix_rules", "{}")
    try:
        return json.loads(raw)
    except Exception:
        return {
            "min_length": 1,
            "max_length": 32,
            "pattern": "^[a-z0-9][a-z0-9._-]*$"
        }
```

### 2.3 `outlook_web/controllers/settings.py` — GET 暴露新 key

**位置**：约 236 行（temp_mail_prefix_rules 之后）

```python
# Cloudflare Worker 独立域名配置（v0.3 Tab 重构）
safe_settings["cf_worker_domains"] = settings_repo.get_cf_worker_domains()
safe_settings["cf_worker_default_domain"] = settings_repo.get_cf_worker_default_domain()
safe_settings["cf_worker_prefix_rules"] = settings_repo.get_cf_worker_prefix_rules()
```

### 2.4 `outlook_web/controllers/settings.py` — PUT 处理新 key

**位置**：约 445 行（temp_mail_prefix_rules 处理之后）

```python
# v0.3: CF Worker 独立域名配置
if "cf_worker_domains" in data:
    try:
        domains = _parse_temp_mail_domains_input(data["cf_worker_domains"])
        queue_setting_update("cf_worker_domains", json.dumps(domains, ensure_ascii=False))
    except ValueError as e:
        errors.append(str(e))

if "cf_worker_default_domain" in data:
    queue_setting_update(
        "cf_worker_default_domain",
        str(data["cf_worker_default_domain"] or "").strip(),
    )

if "cf_worker_prefix_rules" in data:
    try:
        prefix_rules = _parse_temp_mail_prefix_rules_input(data["cf_worker_prefix_rules"])
        queue_setting_update("cf_worker_prefix_rules", json.dumps(prefix_rules, ensure_ascii=False))
    except ValueError as e:
        errors.append("cf_worker_prefix_rules 格式无效")
```

### 2.5 `outlook_web/controllers/settings.py` — 同步接口改写目标 key

**位置**：约 1060 行（api_sync_cf_worker_domains 函数内）

**改动前**：
```python
settings_repo.set_setting("temp_mail_domains", json.dumps(domains_payload, ensure_ascii=False), commit=False)
settings_repo.set_setting("temp_mail_default_domain", default_domain, commit=False)
```

**改动后**：
```python
# v0.3: 同步到独立的 cf_worker_* key，不覆盖 GPTMail 的 temp_mail_* key
settings_repo.set_setting("cf_worker_domains", json.dumps(domains_payload, ensure_ascii=False), commit=False)
settings_repo.set_setting("cf_worker_default_domain", default_domain, commit=False)
```

---

## 3. 前端实现

### 3.1 `templates/index.html` — 设置页面 Tab 结构

**整体结构**（替换现有的 `#page-settings` 内容）：

```html
<!-- ===== Page: Settings ===== -->
<div class="page page-hidden" id="page-settings" style="padding: 1.5rem;">
    <!-- Tab 导航栏 -->
    <div class="settings-tab-nav" id="settingsTabNav">
        <button class="settings-tab active" data-tab="basic" onclick="switchSettingsTab('basic')">基础</button>
        <button class="settings-tab" data-tab="temp-mail" onclick="switchSettingsTab('temp-mail')">临时邮箱</button>
        <button class="settings-tab" data-tab="api-security" onclick="switchSettingsTab('api-security')">API 安全</button>
        <button class="settings-tab" data-tab="automation" onclick="switchSettingsTab('automation')">自动化</button>
    </div>

    <!-- Tab 内容区 -->
    <div class="settings-tab-content">
        
        <!-- Tab 1: 基础 -->
        <div class="settings-tab-pane active" id="settings-tab-basic">
            <div class="card">
                <div class="card-header">
                    <div class="card-title">⚙️ 基础设置</div>
                </div>
                <div class="card-body" style="padding: 1.5rem;">
                    <div class="form-group">
                        <label class="form-label">登录密码</label>
                        <input type="password" class="form-input" id="settingsPassword" placeholder="输入新密码（留空则不修改）">
                        <div class="form-hint">用于登录系统的密码</div>
                    </div>
                    <!-- 保存按钮 -->
                    <div style="margin-top: 1.5rem;">
                        <button class="btn btn-primary" onclick="saveSettings()">保存设置</button>
                    </div>
                </div>
            </div>
        </div>

        <!-- Tab 2: 临时邮箱 -->
        <div class="settings-tab-pane" id="settings-tab-temp-mail">
            <!-- Provider 单选按钮组 -->
            <div class="card" style="margin-bottom: 1rem;">
                <div class="card-body" style="padding: 1rem;">
                    <div class="form-group" style="margin-bottom: 0;">
                        <label class="form-label">全局临时邮箱 Provider</label>
                        <div class="provider-radio-group">
                            <label class="provider-radio">
                                <input type="radio" name="tempMailProvider" value="legacy_bridge" onchange="onTempMailProviderChange('legacy_bridge')">
                                <span class="provider-radio-label">
                                    <span class="provider-name">🔧 GPTMail (Legacy Bridge)</span>
                                    <span class="provider-desc">自建域名临时邮箱服务</span>
                                </span>
                            </label>
                            <label class="provider-radio">
                                <input type="radio" name="tempMailProvider" value="cloudflare_temp_mail" onchange="onTempMailProviderChange('cloudflare_temp_mail')">
                                <span class="provider-radio-label">
                                    <span class="provider-name">☁ Cloudflare Temp Mail</span>
                                    <span class="provider-desc">CF Worker 部署的临时邮箱</span>
                                </span>
                            </label>
                        </div>
                    </div>
                </div>
            </div>

            <!-- GPTMail 配置面板 -->
            <div class="card" id="gptmailConfigPanel">
                <div class="card-header">
                    <div class="card-title">🔧 GPTMail 配置</div>
                </div>
                <div class="card-body" style="padding: 1.5rem;">
                    <!-- 现有 GPTMail 字段 -->
                    ...
                </div>
            </div>

            <!-- CF Worker 配置面板 -->
            <div class="card" id="cfWorkerConfigPanel" style="display: none;">
                <div class="card-header">
                    <div class="card-title">☁ Cloudflare Temp Email Worker 配置</div>
                </div>
                <div class="card-body" style="padding: 1.5rem;">
                    <!-- CF Worker 字段 + 只读域名 -->
                    ...
                </div>
            </div>
        </div>

        <!-- Tab 3: API 安全 -->
        <div class="settings-tab-pane" id="settings-tab-api-security">
            <!-- 对外 API + 公网安全配置 -->
            ...
        </div>

        <!-- Tab 4: 自动化 -->
        <div class="settings-tab-pane" id="settings-tab-automation">
            <!-- Token 刷新 + 轮询 + Email 通知 -->
            ...
        </div>

    </div>
</div>
```

### 3.2 CSS 样式 `static/css/main.css`

**新增样式**：

```css
/* Tab 导航栏 */
.settings-tab-nav {
    display: flex;
    gap: 0;
    border-bottom: 2px solid var(--border-light);
    margin-bottom: 1.5rem;
    overflow-x: auto;
}

.settings-tab {
    padding: 0.75rem 1.5rem;
    background: none;
    border: none;
    font-size: 0.95rem;
    font-weight: 500;
    color: var(--text-muted);
    cursor: pointer;
    position: relative;
    transition: color 0.2s;
}

.settings-tab:hover {
    color: var(--text);
}

.settings-tab.active {
    color: var(--clr-primary);
    font-weight: 600;
}

.settings-tab.active::after {
    content: '';
    position: absolute;
    bottom: -2px;
    left: 0;
    right: 0;
    height: 2px;
    background: var(--clr-primary);
}

/* Tab 内容区 */
.settings-tab-pane {
    display: none;
}

.settings-tab-pane.active {
    display: block;
}

/* Provider 单选按钮组 */
.provider-radio-group {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
}

.provider-radio {
    flex: 1;
    min-width: 200px;
    cursor: pointer;
}

.provider-radio input[type="radio"] {
    display: none;
}

.provider-radio-label {
    display: flex;
    flex-direction: column;
    padding: 1rem;
    border: 2px solid var(--border-light);
    border-radius: 8px;
    transition: all 0.2s;
}

.provider-radio input[type="radio"]:checked + .provider-radio-label {
    border-color: var(--clr-primary);
    background: var(--bg-secondary);
}

.provider-name {
    font-weight: 600;
    font-size: 0.95rem;
}

.provider-desc {
    font-size: 0.82rem;
    color: var(--text-muted);
    margin-top: 0.25rem;
}

/* 只读字段样式 */
.readonly-field {
    background: var(--bg-secondary) !important;
    border-color: var(--border-light) !important;
    cursor: default !important;
    color: var(--text-muted);
}

.readonly-field::after {
    content: '🔒';
    position: absolute;
    right: 12px;
    top: 50%;
    transform: translateY(-50%);
}

/* 移除旧设置页宽度限制 */
#page-settings {
    max-width: none !important;
}

/* Tab 自动保存状态圆点指示器 */
.settings-tab.saving::after {
    /* 覆盖 active 状态的底线，改为圆点指示 */
}
.settings-tab.saving > span.tab-save-dot,
.settings-tab.save-error > span.tab-save-dot {
    display: inline-block;
    width: 6px;
    height: 6px;
    border-radius: 50%;
    margin-left: 6px;
    vertical-align: middle;
}
.settings-tab.saving > span.tab-save-dot {
    background: var(--clr-warn);
}
.settings-tab.save-error > span.tab-save-dot {
    background: var(--clr-danger);
}
```

### 3.3 `static/js/main.js` — 核心逻辑

#### 3.3.1 Tab 切换与自动保存

```javascript
// 当前激活的 Tab（默认 basic）
let currentSettingsTab = 'basic';

// Tab 切换函数
function switchSettingsTab(tabName) {
    const prevTab = currentSettingsTab;
    if (prevTab === tabName) return; // 同一 Tab 无操作
    currentSettingsTab = tabName;

    // 1. 基础 Tab 切走时，密码框有内容则清空 + Toast 提示（5 秒自动消失）
    if (prevTab === 'basic') {
        const pwdEl = document.getElementById('settingsPassword');
        if (pwdEl && pwdEl.value.trim()) {
            pwdEl.value = '';
            showToast('密码修改未保存，如需修改请在「基础」Tab 重新输入后点击保存', 'warning');
        }
    }

    // 2. 立即更新 Tab 按钮视觉状态（不等待保存）
    document.querySelectorAll('.settings-tab').forEach(btn => {
        btn.classList.toggle('active', btn.dataset.tab === tabName);
    });

    // 3. 立即更新 Tab 内容区显隐（不等待保存）
    document.querySelectorAll('.settings-tab-pane').forEach(pane => {
        pane.classList.toggle('active', pane.id === `settings-tab-${tabName}`);
    });

    // 4. 后台异步触发自动保存（不阻塞 UI）
    if (prevTab !== 'basic') {
        autoSaveSettings(prevTab);
    }
}

// 自动保存逻辑（密码除外）
async function autoSaveSettings(tabName) {
    if (tabName === 'basic') return; // 基础 Tab 不触发自动保存

    const settings = {};

    if (tabName === 'temp-mail') {
        // 临时邮箱 Tab：只保存当前 Provider 面板的字段
        const provider = document.querySelector('input[name="tempMailProvider"]:checked')?.value || 'legacy_bridge';
        settings.temp_mail_provider = provider;

        if (provider === 'legacy_bridge') {
            // GPTMail 面板字段
            settings.temp_mail_api_base_url = getValueOrNull('settingsTempMailApiBaseUrl');
            settings.temp_mail_api_key = getValueOrNull('settingsTempMailApiKey');
            settings.temp_mail_default_domain = getValueOrNull('settingsTempMailDefaultDomain');
            // ... 其他 GPTMail 字段（domains, prefix_rules）
        } else {
            // CF Worker 面板字段（只读域名字段不写入）
            settings.cf_worker_base_url = getValueOrNull('settingsCfWorkerBaseUrl');
            settings.cf_worker_admin_key = getValueOrNull('settingsCfWorkerAdminKey');
            settings.cf_worker_prefix_rules = parseJsonOrNull('settingsCfWorkerPrefixRules');
            // settingsCfWorkerDomains / settingsCfWorkerDefaultDomain 不写入
        }
    }

    // API 安全、自动化 Tab 类似处理...

    if (Object.keys(settings).length === 0) return;

    // 显示保存中圆点（在上一个 Tab 按钮上）
    const prevTabBtn = document.querySelector(`.settings-tab[data-tab="${tabName}"]`);
    let dotEl = null;
    if (prevTabBtn) {
        dotEl = document.createElement('span');
        dotEl.className = 'tab-save-dot';
        prevTabBtn.appendChild(dotEl);
        prevTabBtn.classList.add('saving');
    }

    try {
        const resp = await fetch('/api/settings', {
            method: 'PUT',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify(settings)
        });
        if (!resp.ok) throw new Error(`HTTP ${resp.status}`);
        // 保存成功：移除圆点
        if (prevTabBtn) {
            prevTabBtn.classList.remove('saving');
            if (dotEl) dotEl.remove();
        }
    } catch (e) {
        // 保存失败：圆点变红保留 + 持久 Toast
        if (prevTabBtn) {
            prevTabBtn.classList.remove('saving');
            prevTabBtn.classList.add('save-error');
        }
        showToast(`保存失败，[${tabName}] Tab 的修改尚未保存，请手动重试`, 'error', null, true);
    }
}
```

#### 3.3.2 Provider 切换面板显隐

```javascript
function onTempMailProviderChange(provider) {
    const gptmailPanel = document.getElementById('gptmailConfigPanel');
    const cfWorkerPanel = document.getElementById('cfWorkerConfigPanel');
    
    if (provider === 'legacy_bridge') {
        gptmailPanel.style.display = 'block';
        cfWorkerPanel.style.display = 'none';
    } else {
        gptmailPanel.style.display = 'none';
        cfWorkerPanel.style.display = 'block';
        
        // 加载 CF Worker 的只读域名字段（如果已同步过）
        loadCfWorkerReadonlyFields();
    }
}
```

#### 3.3.3 同步后只读状态管理

```javascript
// 同步成功后更新只读字段
function updateCfWorkerReadonlyFields(data) {
    const domainsEl = document.getElementById('settingsCfWorkerDomains');
    const defaultDomainEl = document.getElementById('settingsCfWorkerDefaultDomain');
    const syncTimeEl = document.getElementById('cfWorkerSyncTime');
    
    if (domainsEl && data.domains) {
        domainsEl.value = JSON.stringify(
            data.domains.map(d => ({ name: d, enabled: true })),
            null, 2
        );
        domainsEl.classList.add('readonly-field');
        domainsEl.readOnly = true;
    }
    
    if (defaultDomainEl && data.default_domain) {
        defaultDomainEl.value = data.default_domain;
        defaultDomainEl.classList.add('readonly-field');
        defaultDomainEl.readOnly = true;
    }
    
    if (syncTimeEl) {
        syncTimeEl.textContent = `上次同步：${new Date().toLocaleString()}`;
        syncTimeEl.style.display = 'block';
    }
}
```

#### 3.3.4 loadSettings 加载新 key

```javascript
// 在 loadSettings() 中新增加载逻辑
const cfWorkerDomainsEl = document.getElementById('settingsCfWorkerDomains');
if (cfWorkerDomainsEl) {
    const domains = data.settings.cf_worker_domains || [];
    cfWorkerDomainsEl.value = domains.length ? JSON.stringify(domains, null, 2) : '';
    // CF Worker 域名字段始终只读（永久只读，与 FD §5.3 一致）
    cfWorkerDomainsEl.classList.add('readonly-field');
    cfWorkerDomainsEl.readOnly = true;
    // 空值时 placeholder 文字引导用户同步
    if (!domains.length) {
        cfWorkerDomainsEl.setAttribute('placeholder', '尚未同步，请点击上方按钮同步');
    }
}

const cfWorkerDefaultDomainEl = document.getElementById('settingsCfWorkerDefaultDomain');
if (cfWorkerDefaultDomainEl) {
    cfWorkerDefaultDomainEl.value = data.settings.cf_worker_default_domain || '';
    // CF Worker 默认域名字段始终只读（永久只读，与 FD §5.3 一致）
    cfWorkerDefaultDomainEl.classList.add('readonly-field');
    cfWorkerDefaultDomainEl.readOnly = true;
    if (!cfWorkerDefaultDomainEl.value) {
        cfWorkerDefaultDomainEl.setAttribute('placeholder', '尚未同步');
    }
}

// 设置 Provider 单选按钮选中状态
const provider = data.settings.temp_mail_provider || 'legacy_bridge';
const radioBtn = document.querySelector(`input[name="tempMailProvider"][value="${provider}"]`);
if (radioBtn) radioBtn.checked = true;
if (typeof onTempMailProviderChange === 'function') {
    onTempMailProviderChange(provider);
}
```

---

## 3.5 `static/js/main.js` — showToast 扩展（持久模式）

**现状**：`showToast(message, type, errorDetail)` 写死了 duration（3000ms / 8000ms），不支持持久显示。

**改动**：新增第 4 个可选参数 `persistent = false`，当为 `true` 时不设 setTimeout 自动消失，改为在 Toast 内追加 × 关闭按钮。

**改动位置**：约 1169 行，`function showToast(...)` 函数体内。

```javascript
function showToast(message, type = 'info', errorDetail = null, persistent = false) {
    // ... 现有 toast 创建逻辑 ...

    // 新增：persistent 时追加关闭按钮
    if (persistent) {
        const closeBtn = document.createElement('button');
        closeBtn.textContent = '×';
        closeBtn.style.cssText = 'background:none;border:none;color:inherit;cursor:pointer;margin-left:12px;font-size:1.1rem;opacity:0.8;';
        closeBtn.onclick = () => {
            toast.style.opacity = '0';
            setTimeout(() => toast.remove(), 300);
        };
        toast.appendChild(closeBtn);
        // 不设置 setTimeout 自动消失
    } else {
        const duration = (errorDetail && type === 'error') ? 8000 : 3000;
        setTimeout(() => {
            toast.style.opacity = '0';
            toast.style.transform = 'translateX(30px)';
            setTimeout(() => toast.remove(), 300);
        }, duration);
    }
}
```

**调用方式**（自动保存失败时）：
```javascript
showToast(`保存失败，[${tabName}] Tab 的修改尚未保存，请手动重试`, 'error', null, true);
```

---

### 4.1 后端验收

- [ ] `db.py` 新增 3 个 settings key 默认值
- [ ] `settings.py` 新增 3 个 getter 函数
- [ ] `controllers/settings.py` GET 接口返回 `cf_worker_domains`、`cf_worker_default_domain`、`cf_worker_prefix_rules`
- [ ] `controllers/settings.py` PUT 接口接受并保存 3 个新 key
- [ ] `api_sync_cf_worker_domains` 同步目标改为 `cf_worker_*` key

### 4.2 前端验收

- [ ] 设置页面显示 4 个 Tab：基础、临时邮箱、API 安全、自动化
- [ ] 点击 Tab 切换内容区，无需刷新
- [ ] 当前 Tab 有蓝色下划线视觉标识
- [ ] 临时邮箱 Tab 显示 Provider 单选按钮组
- [ ] 选中 GPTMail 时 GPTMail 面板可见，CF Worker 面板隐藏
- [ ] 选中 CF Worker 时 CF Worker 面板可见，GPTMail 面板隐藏
- [ ] 切换 Provider 立即响应，无需保存
- [ ] CF Worker 同步后域名字段变为只读（灰色背景 + readOnly）
- [ ] 切换 Tab 时自动保存上一个 Tab 的修改（密码除外）
- [ ] 页面宽度不受 720px 限制

### 4.3 数据隔离验收

- [ ] GPTMail 域名存储在 `temp_mail_domains`
- [ ] CF Worker 域名存储在 `cf_worker_domains`
- [ ] 切换 Provider 后两套配置互不覆盖

---

*记录人：AI 助手 | 2026-04-04*
